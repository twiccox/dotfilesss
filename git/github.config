[user]
	username = twiccox
	name = Alan Chang
	email = twiccox@gmail.com

	# TODO: add GPG key ID
	# NOTE: GPG key ID could be in 8-digit (short) format.
	# To find the key ID, run:
	# $ gpg --list-secret-keys --keyid-format SHORT
	signingKey = [GPG key ID]
[init]
	defaultBranch = main
